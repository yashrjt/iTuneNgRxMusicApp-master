/*
import * as fromMusic from './music.action';

describe('Music Actions', () => {
  describe('GetMusic Actions', () => {
    describe('GetMusic', () => {
      it('should create an action', () => {
        const payload = 'Jackson';
        const action = new fromMusic.GetMusic('jackson');
        expect({...action}).toEqual({
          type: fromMusic.GET_MUSIC,
          payload
        });
      });
    });
  });
 });
*/
